# different scraping practices

To install npm package dependencies `npm install`

For youtube scraper make sure you install ffmpeg in your OS too.

To run any of the scripts: `node nameOfTheScript'`